pipeline {
    agent {
        label "build-in"
    }
    tools {
        jdk "jdk17"
    }
    stages {
        stage("Build") {
            steps {
                sh "java -version"
            }
        }
    }
}
