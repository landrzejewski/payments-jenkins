pipeline {
    agent {
        label "built-in"
    }
    tools {
        jdk "jdk17"
    }
    stages {
        stage("Build") {
            steps {
                sh "java -version"
            }
        }
    }
}
